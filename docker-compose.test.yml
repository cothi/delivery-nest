services:
  test-db:
    image: mysql:8.0
    container_name: test-db2
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: test
      MYSQL_USER: testuser
      MYSQL_PASSWORD: testpassword
    ports:
      - "3308:3306"
    volumes:
      - test-data:/var/lib/mysql
    restart: always

volumes:
  test-data:
