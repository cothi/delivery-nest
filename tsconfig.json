{
  "compilerOptions": {
    "baseUrl": ".",          // 루트 경로를 프로젝트 루트로 설정
    "paths": {
      "@libs/*": ["libs/*"],  // 공통 라이브러리 경로 설정
      "@apps/*": ["apps/*"]   // 서비스 경로도 추가 설정
    }
  }
}


